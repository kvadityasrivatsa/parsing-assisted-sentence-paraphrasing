# install FairSequence (only for training)
git clone https://github.com/pytorch/fairseq
cd fairseq
python3 -m pip install --editable ./

# return to project directory
cd -

# install dependecies
python3 -m pip install -r requirements.txt

